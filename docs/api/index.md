# API

The public API currently exposes a small sample greeting helper:

- `bibsync::greeting`
- `bibsync::GreetingError`

Replace this page with crate-specific documentation after customizing the
template. Public Rust API documentation is generated by `cargo doc` and
published to docs.rs when the crate is released.
